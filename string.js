let arr = ["Hello", "there", "students", "of", "SEDC", "!"];
let array = ['You', 'guys', 'are', 'great', '!'];

function strings() {
    console.log(array.join(' '));
    console.log(arr.join(' '));
    }

console.log(strings());
alert(array.join(' '));
